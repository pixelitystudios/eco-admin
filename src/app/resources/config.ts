const api = 'http://localhost:3000/api/v1/'

export const config = {
    api: {
        user: {
            save: 'users/edit',
            create: 'users/create',
            get: 'users/find',
            all: 'users'
        }
    }
}